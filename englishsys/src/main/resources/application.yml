server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/english-lesson-db
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf:
    cache: false
  messages:
    basename: messages
